apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: symphonyportal
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: symphonyportal
                port:
                  number: 8000
