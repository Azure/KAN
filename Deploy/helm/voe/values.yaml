# Default values for voe.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
symphonyAgentVersion: "0.41.19"
image:
  pullPolicy: Always
  image: p4etest.azurecr.io/symphonyportal
  tag:
symphonyportal:
  videoSource: opencv
storage:
  storageResourceGroup:
  storageAccount:
  storageContainer:
  subscriptionId:
customvision:
  endpoint:
  trainingKey:
servicePrincipal:
  tenantId:
  clientId:
  clientSecret:
