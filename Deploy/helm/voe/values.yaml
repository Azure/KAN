# Default values for voe.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

installIngress: true

image:
  pullPolicy: Always
  image: intelligentedge/webmodule
  tag: "0.36.0-amd64"
webmodule:
  videoSource: opencv
iothub:
  connectionString: 
